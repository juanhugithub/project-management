# G1 正式库只读预检报告

**执行时间：2026-08-13 22:30:45 +08:00**
**命令：** `python .vibe/evidence/g1-preflight-check.py`
**范围：** `data/project.db`，以 SQLite `mode=ro` URI 打开；无迁移、无写入。

## 零写操作证据

| 项目 | 结果 |
|---|---|
| 打开前 SHA-256 | `7cc1e32002a9f1faca221fb945a171cb90592eef68b37ecabf1584f3d6d087c4` |
| 打开后 SHA-256 | `7cc1e32002a9f1faca221fb945a171cb90592eef68b37ecabf1584f3d6d087c4` |
| 哈希是否一致 | 是 |
| `user_version` | `0`（尚未纳入受控迁移） |
| `journal_mode` | `delete` |

## 当前库基线

共 6 张表：`dict_item` 31 行，`enterprise`、`project`、`funding`、`node` 各 0 行，`system_config` 1 行。`integrity_check=ok`，`foreign_key_check` 为 0 条。归档年度为空；四张业务表尚无软删除字段，这是 M003 的预期输入，不是现有数据违规。

## 不合规记录

以下检查均为 **0 条违规**：企业信用代码重复、金额非负且最多两位小数、日期与起止顺序、项目阶段、项目承担企业、项目唯一键、字典引用、资金状态、节点状态、节点完成日期一致性。

因此当前为空库迁移路径：没有历史业务记录需要转换或修正；G2 仍必须先在备份副本验证 M001--M004，再按迁移协议处理正式库。

## 人工修正与自动化边界

- **本次需要 HUMAN 修正：无。** 空库不存在待治理业务记录。
- **绝不自动修改：** 项目编号、企业/项目匹配、金额、日期、重复记录、归档年度、中止/撤销状态；未来出现违规记录时只报告，等待 HUMAN 明确决定。
- 本报告不等同于迁移授权；正式库在本阶段未被迁移。
