# 科技项目全生命周期台账 — 可信台账硬化计划

> 状态：**可信台账核心已完成开发验收；下一主线为 MCP Agent 数据底座与 Gitee 可回退更新。**
> 制定日期：2026-08-13  
> 适用目录：`E:\Reasonix WorkSpace\科技项目台账`

## 1. 重编排结论

当前系统已具备项目、企业、资金、节点、提醒、统计、Excel 导入、备份和只读 MCP 的界面与基础功能；但它尚未形成可审计、口径统一、能安全恢复的正式台账闭环。

可信台账硬化已完成开发验收。下一阶段将它升级为“**面向任意合规 Agent 的本地 MCP 事实与填表数据底座**”：先稳定 MCP 业务语义、模板数据集和可追溯产物，再接入不同 Agent 的文字与表格任务。AI 只能读取事实、生成草稿或创建待确认导入批次，不能直接写正式台账。

### 1.1 本轮目标

将系统从“可演示、可试用”提升到“单人本地日常台账可控使用”：

1. 所有界面、统计、导出和 MCP 对金额、拨付状态、节点状态使用同一口径。
2. 非法金额、非法日期、无承担企业项目、非法字典值、非法状态流转不能写入数据库。
3. 年度归档同时约束新建、修改、删除、Excel 导入和子表写入，不能被绕过。
4. Excel 导入先校验、再由用户确认、最后原子提交；失败时不留下部分数据。
5. 备份可验证、可恢复；关键写操作可追溯；删除不再是不可恢复的静默损失。
6. 每一个已宣称完成的阶段都有可重复执行的测试、证据和文档状态。

### 1.2 明确不做

- 不改为云端、多用户、审批流或移动端。
- 不接入外部官方系统，不自动抓取数据。
- 不让 MCP 具有写入能力。
- 不为了修复问题引入 Flask、React、ORM、独立数据库服务等技术栈替换。
- 不迁移或修正现有正式数据，除非迁移报告列出具体记录且经 HUMAN 确认。
- 不以“自动修复”“模糊匹配”“静默默认值”掩盖脏数据。

## 2. 已确认问题与修复原则

| 编号 | 问题 | 风险 | 本计划的固定处理原则 |
|---|---|---|---|
| P0-01 | 工作台按“已到账”统计，项目页/MCP 把全部资金显示为“已到位” | 对账、报表、AI 填表口径冲突 | 建立唯一资金口径服务；禁用含糊字段名 |
| P0-02 | 非法金额、任意阶段、无企业项目可直接写入 | 台账数据失真 | 后端与 SQLite 双层约束；非法输入明确 400，不转空值 |
| P0-03 | 已归档年度仍可新建项目 | 年度冻结失效 | 归档规则集中在所有写入口，包含创建、导入和子表 |
| P0-04 | 导入逐行直接写库，可能部分成功、重复或遗留企业 | 批量数据不可控 | 暂存校验、用户确认、单事务提交、可追溯批次 |
| P0-05 | 直接复制活跃 SQLite 文件；自动备份仅启动时触发 | 备份不可证明可恢复 | SQLite 在线备份、校验、恢复演练、可见失败 |
| P0-06 | 硬删除、无审计；MCP/Web 重复业务查询 | 误操作无法追责、后续口径漂移 | 软删除/恢复与审计；共享只读领域查询层 |
| P1-01 | 本地写 API 不验证请求来源；详情渲染存在未统一转义路径 | 本地浏览器攻击面 | 本地会话防护、来源校验、统一文本渲染 |
| P1-02 | 无 Git、无完整测试目录，`.vibe` 状态矛盾且缺 reuse-survey | 无法可靠迭代 | 先建立基线、测试、证据和治理单一事实源 |

## 3. 关键业务契约（实施前冻结）

### 3.1 资金口径

下列概念必须同时保留，任何 API、页面、导出、MCP 不得混用：

| 名称 | 精确定义 | 计算来源 |
|---|---|---|
| 项目总金额 | 项目批准或立项的总金额 | `project.total_amount` |
| 计划拨付额 | 有应拨日期的资金记录金额之和 | `funding.plan_date IS NOT NULL` |
| 已拨付额 | 状态为“已拨付”或“已到账”的资金记录金额之和 | `funding.status IN ('已拨付','已到账')` |
| 已到账额 | 状态为“已到账”的资金记录金额之和 | `funding.status = '已到账'` |
| 待拨额 | 未达到“已拨付”状态的资金金额 | 由状态计算 |
| 逾期未拨额 | 应拨日早于今天且未达到“已拨付”状态的资金金额 | 由日期与状态计算 |

资金勾稽的基准是“各资金来源**计划/批准金额**之和”与项目总金额的差异；到账进度是独立事实，不能替代勾稽基准。若业务上资金记录同时承载“计划”和“实际”两种金额，必须在数据模型评审中确认是否拆为“资金计划”和“资金拨付”两类实体；确认前不得自行臆造字段语义。

### 3.2 项目、企业、阶段与字典

- 项目创建时必须指定存在且未删除的承担企业；企业删除不得使项目成为未关联状态。
- 金额必须是非负、最多两位小数的 Decimal 语义值；数据库不得再以 `REAL` 作为金额真值。
- 日期必须是有效 `YYYY-MM-DD`；开始日期不得晚于结束日期。
- `level`、`category`、`funding_source`、`node_type`、`enterprise_type`、`district` 必须引用有效字典项；停用字典项仍可显示历史值，但不得再用于新增或修改。
- 项目阶段只能按经确认的状态机流转。异常态“中止/撤销”的进入与恢复规则必须在测试中明确。
- 节点完成时必须记录实际完成日期；未完成节点不得有实际完成日期，除非业务契约明确例外。

### 3.3 归档与删除

- 年度归档按项目开始年度定义；归档后禁止该年度项目及其资金、节点的新建、修改、删除和导入。
- 新建项目在保存前检查其年度是否已归档。
- 归档和解除归档均为高风险操作，必须记录审计日志；解除归档需要二次确认和理由。
- 企业、项目、资金、节点的“删除”调整为软删除；默认列表、统计、提醒、MCP 不返回已删除记录。
- 恢复仅允许在引用关系、年度归档、字典有效性都满足时发生；恢复行为必须留痕。

### 3.4 导入与 MCP

- Excel 导入匹配企业的唯一身份为统一社会信用代码；缺失信用代码的行不得自动按名称合并，必须在预览中由 HUMAN 显式选择“新建”或“关联既有企业”。
- 项目去重规则必须由 HUMAN 在实施前确认：建议使用“项目编号/文号 + 承担企业信用代码”作为唯一业务键；无项目编号的记录只能进入待确认队列，不能自动入账。
- MCP 保持只读，只调用共享查询层，不直接复制 SQL 规则。
- MCP 返回值要标明金额口径字段，如 `planned_total`、`disbursed_total`、`received_total`，不得继续使用语义不清的 `funded_total`。

## 4. 总体实施顺序

```text
G0 基线与治理
  -> G1 领域契约与数据迁移设计
  -> G2 核心写入约束与统一查询
  -> G3 归档、删除、审计
  -> G4 可控 Excel 导入
  -> G5 可恢复备份
  -> G6 本地安全与 UI 收口
  -> G7 MCP 一致性、回归与试运行
```

阶段实现均以临时数据库完成验证。正式库迁移仍必须先在副本演练并取得 HUMAN 授权；不得以测试通过替代正式数据迁移授权。

### 4.1 G1 HUMAN 已确认决策（2026-08-13）

- 资金单记录不拆分；一条资金记录同时承载计划日期、实际日期和拨付/到账状态。
- 项目唯一键为“项目编号/文号 + 承担企业统一社会信用代码”；无编号记录不得自动入账。
- 正常流转为：申报中→已立项→实施中→待验收→已验收→绩效跟踪→已完结。
- 中止仅可从已立项、实施中、待验收进入，且不可恢复。
- 撤销为仅人工显式进入的不可恢复终态；具体授权条件是未来待定事项，不阻塞 G1/G2。

权威决策记录见 `docs/decisions/0001-g1-domain-contract.md`。

## 4.2 MCP Agent 数据底座与 Gitee 更新计划（待 HUMAN 确认后实施）

### 目标

让任何获授权 Agent 都能通过稳定的 MCP 协议取得同一份项目事实、业务数据集和填表依据，完成复杂汇报、说明文字、通知草稿和表格填写；同时让应用代码可从 Gitee 低风险更新，而正式数据库永不被 Git 覆盖。

### 固定边界

- MCP 默认只读；Agent 不得修改正式项目、资金、节点、归档或审计事实。
- Agent 产生的报告、Excel、说明文字均为“衍生稿”，不是台账事实；必须保存数据口径、工具参数、生成时间、来源项目编号和人工确认状态。
- 写入类 AI 能力只允许创建“待确认导入批次/草稿”，仍复用既有 HUMAN 确认与原子提交机制。
- `data/`、`backups/`、导入原件、会话密钥和本机配置永久不纳入 Git；更新不得复制、覆盖或重建它们。
- Gitee 远程仓库默认采用私有仓库；发布分支仅接收通过完整检查的提交和带版本号标签。

### G8：MCP 版本化业务契约

**目标**：把当前原子查询工具提升为不会随 SQLite 表结构变化而破坏 Agent 的版本化业务接口。

**工作项**：

1. 定义 MCP 协议版本、统一错误格式、分页、筛选、金额单位、日期时区与可见性字段。
2. 明确区分“原子事实工具”和“业务数据集工具”；所有返回结果携带 `contract_version`、`generated_at`、`filters`、`data_scope`、`money_semantics`。
3. 增加项目事实包、验收风险、资金执行、待补编号、复合风险等只读业务工具。
4. 为每个工具写 JSON 示例、输入约束、可读错误和契约测试；保留旧工具的兼容期，不直接破坏已接入 Agent。

**验收**：同一临时样本在 Web、MCP 原子查询与 MCP 业务数据集中的金额、可见项目、归档/软删除过滤完全一致。

### G9：模板任务与衍生稿溯源

**目标**：让 Agent 不必猜字段和口径，即可按固定模板输出可复核的表格行和文字事实包。

**工作项**：

1. 建立版本化模板定义：字段、来源字段、筛选条件、单位、必填规则、列顺序、校验规则和适用说明。
2. 提供 `list_reporting_templates`、`get_template_schema`、`build_template_dataset`、`validate_filled_template` 等 MCP 工具。
3. 建立衍生稿登记：模板版本、MCP 调用参数、数据快照哈希、来源项目编号、Agent/模型标识、人工确认状态和导出文件路径。
4. 先以“季度资金执行表”和“验收风险清单”做两个端到端样例；不抽象为未被真实使用的万能工作流。

**验收**：对固定模板，两个不同 Agent 使用相同 MCP 参数得到相同结构化数据集；每份导出稿可回溯至具体数据快照和筛选条件。

### G10：受控文档解析与 Agent 协作

**目标**：让立项通知、拨款文件、验收材料成为“待确认候选”，而不是让模型直接改账。

**工作项**：

1. 定义文档解析任务输入、允许的文件类型、字段置信/缺失标识和来源页码。
2. Agent 只能输出结构化候选；系统将其映射为既有导入暂存批次。
3. 对项目编号、信用代码、金额、日期、阶段等关键字段执行确定性规则校验；不确定内容必须保留为空并提示 HUMAN。
4. 记录原件哈希、解析 Agent、模板/提示版本、候选结果与最终确认差异。

**验收**：一份含缺失字段的材料只能进入预览，不得产生正式业务记录；人工确认后可追溯原件、候选和最终数据。

### G11：Gitee 发布与热更新

**目标**：在不覆盖正式数据库的前提下，完成代码更新、迁移演练、失败回退与可追溯发布。

**工作项**：

1. 建立 `.gitignore` 与发布清单守卫：禁止 `data/`、备份、导入档案、密钥、本机配置进入提交或发布包。
2. 建立版本文件、发布标签和 `stable` 分支；Gitee 仅作为代码与已审计发布物来源。
3. 实现 Windows 更新器：检查远程版本 → 展示变更 → SQLite online backup → 校验更新包 → 快进更新代码 → 对数据库副本演练迁移 → HUMAN 确认后执行版本化迁移 → 重启服务并运行健康检查。
4. 更新失败时回退代码版本；数据库不做自动降级，使用更新前在线备份恢复副本并由 HUMAN 决定正式恢复。
5. 首次仅支持“本机手动点击检查/更新”；不做后台静默更新或自动删除旧版本。

**验收**：使用临时库和临时 Git 远程模拟一次升级与一次失败回退；源库 SHA、备份、迁移版本、应用版本和健康检查结果均有证据。正式库更新必须单独由 HUMAN 授权。

### G12：真实 Agent 互操作与试运行

**目标**：以真实文字与表格任务证明接口是“任何 Agent 可用”，而非只对开发者可用。

**工作项**：

1. 选择至少两个不同 Agent 客户端，完成同一 MCP 工具发现、事实包、模板数据集和文稿起草任务。
2. 准备脱敏样例：资金执行报告、验收风险清单、项目情况说明、指定 Excel 表头填写。
3. 对每次输出人工核对项目数、金额、日期、来源编号和文书事实；记录发现的问题，不用模型猜测补救。
4. 完成 Gitee 更新演练与备份恢复演练后，进行一周日常录入、导入、查询、Agent 填表试运行。

**验收**：两种 Agent 都只能发现只读工具；同一模板数据集结构一致；至少四类真实任务的事实核对通过；试运行问题进入可追溯清单。

### HUMAN 待确认事项

1. Gitee 采用私有仓库是否确认；仓库创建、成员/密钥配置由谁执行。
2. 第一批两个正式模板是否采用“季度资金执行表”和“验收风险清单”。
3. 允许接入的 Agent 范围：仅本机 MCP 客户端，还是后续允许单位内受控网络客户端。
4. 正式数据库迁移和正式版本更新的执行窗口及确认人。

## 4.3 D13：Windows 傻瓜式部署与本地 Agent 接入（待 HUMAN 确认后实施）

### 目标

使普通单位工作人员在无 Python、Git、MCP、Cloudflare 等开发环境的 Windows 电脑上完成“下载/双击安装 → 双击启动 → 本地备份与更新可用”。本地数据库是唯一事实源，程序更新绝不覆盖它。

### 默认交付模型

```text
安装包（程序与运行时）
  → %LOCALAPPDATA%\科技项目台账\app\<version>
正式数据与本机配置
  → %LOCALAPPDATA%\科技项目台账\data、backups、imports、config
桌面快捷方式
  → 启动当前版本；首次运行自动初始化与健康检查
```

- 默认“当前用户安装”，不要求管理员权限；若单位策略禁止执行用户目录程序，安装器明确报告并提示管理员部署，不尝试绕过策略。
- 内置 Python 运行时、Python 依赖和静态资源；终端用户无需安装 Python、Git 或 `pip`。
- 本地 MCP 默认关闭，仅在用户主动启用本机 Agent 接入时生成/读取本机 Token；不自动创建公网隧道。
- Gitee 更新器只下载/切换程序版本；更新前在线备份，数据库、原件、备份、Token 和本机配置永久独立。

### 工作项

1. 编写构建脚本，使用现有 PyInstaller 生成带运行时的 Windows 程序目录；明确收集 MCP、模板、迁移和静态资源。
2. 建立安装启动器：检测旧版、磁盘空间、数据目录、端口和迁移版本；缺少的应用运行时由安装包补齐，系统级权限不足明确报错。
3. 将数据路径从代码目录迁移到稳定用户数据目录；首次升级只复制/迁移一次，保留原目录且生成可核对迁移报告。
4. 生成桌面快捷方式、开始菜单入口、备份入口、卸载入口和“诊断报告”入口；卸载仅移除程序，默认保留数据与备份。
5. 安装后执行健康检查：启动本地服务、访问首页、检查 SQLite、检查模板/迁移版本、创建并验证一次备份。
6. 用全新 Windows 用户目录完成一次“无 Python 环境”的安装、启动、录入、备份、升级和卸载保留数据验收。

### 验收

- 在干净临时用户目录中，不安装 Python/Git 的前提下完成安装与启动。
- 更新前后正式库 SHA 仅能因已确认迁移改变；应用更新不得删除、覆盖或重建既有数据库。
- 卸载后数据、备份和导入原件仍存在；重新安装可识别并继续使用。
- 一键诊断报告能明确展示应用版本、数据库版本、备份状态、MCP 本机状态与失败原因。

### 人工确认点

默认按“当前用户、无需管理员权限、数据目录位于 `%LOCALAPPDATA%`”实现。若单位要求安装到 `Program Files`、全员共享数据目录或统一管理员安装，需在实现前改为单位 IT 部署方案。

## 4.4 D14：政务台账高级极简前端改造（待 HUMAN 确认后实施）

### 设计方向

以已提供的 StoryForge 设计参考为视觉基准，采用“暖白纸张底色 + 近黑主色 + 极细灰线 + 克制状态绿”的高级极简工作台。核心记忆点为：半透明吸顶导航、巨型但克制的工作台数据、带细网格纹理的高呼吸感页面，以及仅对辅助信息使用渐进披露。

### 政务台账适配规则

- 项目阶段、资金口径、归档状态、删除/恢复、错误信息和主要操作始终可见；不得因 Hover 或动画隐藏。
- 仅将说明文字、低频快捷操作和卡片详情放入渐进披露，键盘焦点也必须可展开同等内容。
- 使用中文优先的本机字体栈，不以仅覆盖拉丁字符的 Inter 作为唯一字体；数字指标采用紧凑等宽数字样式。
- 黑色只用于主操作与核心数据；绿/黄/红只表达明确业务状态，不能作为装饰色。
- 继续支持 1024×768 与 1366×768 办公显示器；小屏时导航和统计自动纵向重排。

### 工作项

1. 在 `static/style.css` 建立可复用设计 Token：颜色、边界、间距、阴影、动效曲线、网格底纹、字号和焦点状态。
2. 改造顶部导航、工作台 Hero、资金与风险指标、核心功能入口、列表空状态和弹窗；所有数据继续来自既有 API。
3. 重排项目详情：总金额、计划拨付、已拨付、已到账、待拨和勾稽状态作为首屏事实卡；异常状态紧邻字段说明。
4. 改造表格、筛选与表单：加强行间可读性、状态标签、字段级错误、键盘焦点和危险操作确认。
5. 做浏览器视觉验收：1366×768、1024×768、数据密集页、空状态、错误状态、归档状态和键盘导航。

### 验收

- 前端语法检查通过，既有 API/业务回归不变。
- 在两个办公分辨率下，任何关键数据、主按钮或错误信息不被遮挡或必须 Hover 才可见。
- 关键流程（新增项目、查看资金口径、确认导入、备份、更新）具备清晰视觉层级与键盘焦点。
- 由 HUMAN 对照提供的参考稿进行一次视觉验收。

## 5. 分阶段任务清单

### G0：基线、治理与可重复验证

**目标**：建立后续每次改动都可比较、可回滚、可证明的工程基线。

**工作项**：

1. 初始化 Git 仓库；首个提交只纳入源代码、设计文档、治理文件和测试，不纳入 `data/project.db`、备份、导出文件和真实导入原件。
2. 新建 `tests/`，以独立临时数据库运行测试，严禁测试接触 `data/project.db`。
3. 把本次已复现问题写成失败测试：金额文本、任意阶段、无企业项目、归档年度新建、资金口径分歧、导入部分提交。
4. 增加 `scripts/check.py`，统一执行 Python 语法检查、前端语法检查、单元/API 测试、SQLite 完整性检查和 `.vibe` 校验。
5. 补齐 `.vibe/reuse-survey.md`：明确本轮不更换 SQLite/标准库/原生前端的边界和原因；同步 `stage-gates.md`、`task-graph.json` 与真实状态。
6. 建立 `docs/decisions/`，记录金额口径、归档语义、删除语义和导入身份规则的决定。

**验收**：

```text
python scripts/check.py
python C:\Users\Administrator\.codex\skills\vibe-engineer\scripts\validate_vibe_project.py "E:\Reasonix WorkSpace\科技项目台账"
```

全部通过；测试执行过程不得改变正式数据库文件的哈希值。

### G1：领域契约确认与迁移设计

**目标**：先确定不能猜测的业务语义，再设计数据变更，避免用代码替用户作财政规则决定。

**HUMAN 必须确认的三项决定**：

1. 单条资金记录是否同时表示计划与实际；若是，是否拆分“资金计划/资金拨付”。
2. 项目业务唯一键是否采用“项目编号/文号 + 企业信用代码”；无编号历史项目如何治理。
3. 项目状态机允许的正常流转、异常态进入条件、异常态是否可恢复。

**工作项**：

1. 更新 `设计方案.md`、`.vibe/domain-model.md`、`.vibe/architecture.md`，让其成为字段语义和状态规则的唯一说明。
2. 设计版本化 SQLite 迁移框架：迁移号、前置检查、事务、完成标记、失败回滚、迁移报告。
3. 将金额存储方案确定为“整数分”或 SQLite `TEXT` Decimal；推荐整数分并在展示层统一转换为万元，避免二进制浮点误差。
4. 设计 `audit_log`、软删除字段、导入批次/暂存表、系统操作配置表结构。
5. 对现有数据库做只读预检，输出不合规记录报告；若没有正式数据，报告仍需证明空库迁移路径。

**验收**：

- HUMAN 签字确认三项决策。
- 迁移方案含“升级、失败回滚、降级不可用时的恢复方法”。
- 预检报告明确列出：不合规记录、是否需要人工修正、不会自动修改的内容。

### G2：核心写入约束与统一查询层

**目标**：让错误状态无法入库，并让 Web、导出、MCP 共享同一口径。

**工作项**：

1. 新建 `ledger/` 领域层，按职责拆分：
   - `validation.py`：金额、日期、字典、状态流转和关联校验；
   - `services.py`：项目/企业/资金/节点的事务性写入；
   - `queries.py`：项目全貌、资金进度、提醒、统计和导出查询；
   - `money.py`：金额转换、舍入和显示；
   - `errors.py`：可读且可定位的领域错误。
2. `app.py` 只保留 HTTP 路由、请求解析和响应映射，所有写入调用 `services.py`，所有读取调用 `queries.py`。
3. `mcp_server.py` 删除重复 SQL，改为调用 `queries.py`，并更新工具描述与返回字段。
4. 数据库添加必要的外键、`NOT NULL`、`CHECK`、唯一索引和触发器；数据库约束用于保护直接 SQL/迁移路径，Python 校验用于返回清晰错误。
5. 写 API 对非法金额、非法日期、非法字典、非法阶段、企业不存在或停用、归档限制返回明确 `400/403/409`，绝不把非法值静默转换为 `NULL`。
6. 统一项目详情、工作台、统计、CSV 导出、MCP 的资金口径字段和中文标签。

**验收**：

- 使用 HTTP API、导入服务、直接 SQLite 写入三种路径，分别验证不变量无法被突破。
- 同一组样本数据下，项目详情、工作台、统计、CSV、MCP 的计划/已拨/到账值逐项相等。
- `pytest -q` 覆盖每个被禁止状态和每种正确状态。

### G3：归档、软删除与审计

**目标**：让关键操作能冻结、可解释、可恢复。

**工作项**：

1. 将归档规则置于 `services.py`，统一覆盖项目、资金、节点、导入和恢复路径。
2. 新建、修改、删除、归档、解除归档、导入确认、恢复、备份恢复写入审计日志：操作者标识（本地用户名或固定操作者）、时间、对象、操作、前后摘要、理由和来源批次。
3. 实现软删除和“最近删除”恢复视图；默认查询不返回已删除记录。
4. 前端针对归档、解除归档、删除、恢复显示清晰的影响说明和二次确认；解除归档与恢复需要填写理由。
5. 增加只读审计查询与按时间导出功能；MCP 默认不暴露敏感审计详情，除非后续单独授权。

**验收**：

- 归档年度的所有写入口均被拒绝，包含 POST、PUT、DELETE、Excel 导入和恢复。
- 删除后默认列表/统计/MCP 不包含记录；恢复后数据与关联完整。
- 每种高风险操作均生成一条可读审计记录。

### G4：可控 Excel 导入

**目标**：从“直接灌库”改为“可审阅、可确认、可回滚”的批量入账流程。

**工作项**：

1. 将导入拆为：上传解析 → 暂存校验 → 预览差异 → HUMAN 确认 → 单事务提交 → 审计归档。
2. 上传文件生成 SHA-256、原始文件名、导入时间和字段映射版本；原文件保存到受控导入档案目录，禁止覆盖。
3. 每行输出确定性结论：新建企业、新建项目、更新候选、重复、缺失身份、字段错误、归档冲突；禁止按企业名称模糊合并。
4. 提交前显示“将新建/将更新/将跳过/将失败”计数和行号；有阻断错误时不能确认提交。
5. 项目更新只允许在 HUMAN 明确选择“更新模式”且业务唯一键精确匹配时执行；默认仅新增。
6. 全部正式写入在一个事务内完成；任一异常回滚整个批次，不产生孤儿企业、半条项目或审计残留。
7. 提供“撤销最近导入批次”，仅在后续未改动、未归档、审计完整时允许；不满足条件则拒绝并说明原因。

**验收**：

- 合法样例完整导入；一行错误的样例在确认前零写入；提交异常时零部分写入。
- 同一文件二次导入显示重复或精确更新候选，不产生重复项目。
- 预览数据、提交结果、审计记录和数据库结果可一一对应。
- 人工检查导入结果页、错误工作簿与撤销流程。

### G5：可恢复备份

**目标**：证明“可备份”不只是复制出一个文件，而是真正可恢复。

**工作项**：

1. 用 SQLite online backup API 生成时间戳备份，写入临时文件后完整性检查通过再原子发布。
2. 备份命名包含时间、应用版本、数据库 schema 版本和校验值；同秒备份不得覆盖。
3. 自动备份改为常驻服务内按日调度，且保留启动检查；失败必须在页面与日志可见，不能 `except: pass`。
4. 备份保留策略从“无提示删除”改为明确列出待清理文件和规则；首次启用自动清理需 HUMAN 确认。
5. 提供恢复演练命令：在临时目录恢复、执行 `integrity_check`、运行关键查询、比对核心记录数量和审计尾部。
6. 编写 `docs/恢复操作手册.md`，明确正式恢复前必须先全量备份当前数据库、停止服务、恢复到副本验证、由 HUMAN 决定替换。

**验收**：

- 并发写入期间备份文件通过 `PRAGMA integrity_check`。
- 从备份恢复到临时目录后，关键表数量、抽样记录、统计和审计尾部与备份时快照一致。
- 自动备份故障会被记录并可在 UI 看到；不会静默跳过。

### G6：本地安全与 UI 收口

**目标**：使单人本地界面在日常办公环境中不易误操作、口径清晰、可读性足够。

**工作项**：

1. 为写 API 添加本地会话防护：启动时生成进程内随机令牌；前端通过同源请求携带；服务端拒绝缺失/错误令牌、非 `127.0.0.1` Host 和非同源 Origin/Referer 的写操作。
2. 静态服务增加基本安全响应头；所有用户文本经统一 `escapeHtml` 输出，移除未经转义的详情字段插值。
3. 统一错误反馈：字段级错误留在表单附近，事务级错误显示操作编号，可复制到支持材料；不得只显示“失败”。
4. 调整至适合政务办公的基础字号、按钮尺寸、键盘焦点和弹窗语义；至少验证 1366×768 与 1024×768。
5. 项目详情明确展示“项目总金额 / 计划拨付 / 已拨付 / 已到账 / 待拨 / 勾稽状态”，删除“已到位”模糊标签。
6. 统计/导出页面展示统计口径；CSV 导出对以 `=`, `+`, `-`, `@` 开头的文本进行安全转义。

**验收**：

- 跨来源写请求、缺令牌写请求、篡改令牌写请求均失败；正常页面写入仍通过。
- 输入包含 HTML/脚本特征的文本，只能作为文本显示，不能执行。
- 关键流程可仅用键盘完成；1366×768 与 1024×768 无横向遮挡和不可达主操作。
- 通过人工视觉验收和浏览器交互验收。

### G7：MCP、回归、试运行与交付

**目标**：以真实日常任务证明系统可用，并形成可交接版本。

**工作项**：

1. MCP 所有查询改用共享查询层；工具清单仍仅含只读工具。
2. 为 MCP 工具增加契约测试：工具列表无写操作、项目详情金额口径一致、归档/软删除默认不可见、错误返回可读。
3. 准备脱敏代表性样例：至少含多层级、三类资金、部分到账、逾期节点、归档项目、导入错误行和软删除记录。
4. 执行完整回归：API、MCP、导入、备份恢复、安全、前端语法、人工 UI 流程。
5. 由 HUMAN 进行不少于一周的试运行：每日录入、一次导入、一次统计导出、一次 MCP 填表初稿、一次备份恢复演练。
6. 试运行中发现的缺陷进入问题清单；只针对已观察到的失败模式硬化，不加入猜测性兜底。
7. 更新 README、MCP 接入指南、恢复手册、设计方案、`.vibe` 状态和变更日志；创建可回滚的发布标签。

**验收**：

- 所有自动化检查通过，且生成可保存的测试报告。
- MCP 工具清单无写工具；同一项目的 Web/MCP/CSV 数值一致。
- HUMAN 完成试运行清单并确认关键台账结果。
- 发布包不含真实数据库、备份、导入原件或敏感日志。

## 6. 文件与责任边界

| 位置 | 责任 | 本轮变化方向 |
|---|---|---|
| `schema.sql` / `migrations/` | 数据结构、约束、版本升级 | 从一次性建表转为受控迁移 |
| `ledger/` | 领域规则、写服务、查询、金额 | 新增，作为唯一业务规则入口 |
| `app.py` | HTTP 路由与本地服务 | 薄化，不再承载重复业务 SQL |
| `mcp_server.py` | MCP 工具声明与参数转换 | 只读，调用共享查询层 |
| `import_excel.py` / `imports/` | 解析、暂存、预览、提交、撤销 | 从直接写库改为批次工作流 |
| `backup.py` | 在线备份、校验、恢复演练 | 从文件复制改为可验证备份 |
| `static/` | 展示、表单、可访问性 | 统一口径、错误提示和安全渲染 |
| `tests/` | 单元、API、MCP、导入、恢复回归 | 新增，使用临时数据库 |
| `docs/decisions/` | HUMAN 决策记录 | 新增 |
| `.vibe/` | 契约、阶段状态、证据 | 与实际验收同步 |

## 7. 验收矩阵

| 验收项 | 自动证据 | 人工证据 |
|---|---|---|
| 资金口径一致 | 同一 fixture 下 API/MCP/CSV 数值比对 | 查看项目详情、工作台与导出 |
| 数据不变量 | 参数化 API/服务/SQLite 约束测试 | 输入非法值确认错误信息可理解 |
| 年度归档 | 所有写入口拒绝测试 | 归档、查看、解除归档流程 |
| 导入原子性 | 故障注入后表计数与哈希不变 | 预览、确认、错误工作簿、撤销 |
| 备份恢复 | 临时恢复库完整性和抽样比对 | 按恢复手册完成一次演练 |
| 安全渲染与写保护 | 令牌/来源/XSS 测试 | 浏览器中确认恶意文本仅显示 |
| MCP 只读与一致性 | 工具清单和查询契约测试 | AI 客户端实际读取并填一份脱敏表 |
| UI 可用性 | 前端语法与关键 DOM/交互测试 | 1366×768、1024×768 人工检查 |

## 8. 风险、禁止事项与升级条件

### 8.1 禁止事项

- 禁止对正式 `data/project.db` 执行未经备份和预检的迁移。
- 禁止在导入校验失败后继续提交部分记录。
- 禁止通过名称模糊匹配企业或项目。
- 禁止为兼容旧数据而在业务逻辑中默默放宽约束。
- 禁止删改审计日志、导入原件、备份和已发布迁移。
- 禁止在未通过 G2 回归前继续新增业务功能。

### 8.2 必须升级给 HUMAN 的情况

- 对金额记录是否拆分计划与实际存在两种合理解释。
- 发现正式数据不满足新不变量，需要修改、合并或舍弃。
- 需要改变项目唯一键、状态机、归档年度归属或资金来源定义。
- 需要清理超过保留期的历史备份、删除导入原件或替换正式数据库。
- 发现一项需求需要扩大到多人、网络访问、云同步或 MCP 写入。

## 9. 建议执行节奏

| 里程碑 | 进入条件 | 退出证据 |
|---|---|---|
| M0：治理基线 | 本 PLAN.md 获确认 | Git、测试骨架、失败复现测试、`.vibe` 校验通过 |
| M1：业务契约 | HUMAN 确认三项关键决定 | 数据迁移设计和预检报告获确认 |
| M2：可信核心 | M1 通过 | G2、G3 的全量自动化测试通过 |
| M3：安全批量与可恢复 | M2 通过 | 导入原子性、备份恢复和安全测试通过 |
| M4：试运行 | M3 通过 | HUMAN 一周试运行验收、文档与发布标签完成 |

## 10. 确认后第一项实施任务

确认本计划后，第一项只执行 **G0-1 至 G0-4**：创建 Git 基线、测试目录、失败复现测试与统一检查脚本；不改业务逻辑、不迁移正式数据库。

完成后交付：

1. 本阶段完成内容；
2. 实现文件结构；
3. 运行方式；
4. 未完成项；
5. 下一阶段建议；
6. 实际执行命令和结果，以及对正式数据库未改动的证明。
