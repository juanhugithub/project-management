# 科技项目全生命周期台账系统 — 设计方案

> 版本 v0.2（供审查）· 2026-08
> 定位：**本地单人使用**的科技项目台账工具，管理「企业 / 项目 / 财政资金」三维信息与全生命周期。

---

## 1. 一页看懂

- **给谁用**：你一个人（昆山市科技局项目管理人员）。
- **解决什么**：把分散在各级官方系统、Excel、纸质材料里的国/省/市/本级科技项目信息，收拢成**一套本地台账**，做到——查全貌快、节点不漏提醒、资金勾稽不出错、随时能出统计报表。
- **长什么样**：一个本地文件夹，**双击启动 → 自动打开浏览器 → 在本地网页里操作**，数据存在本机一个数据库文件里，不联网、不上云。
- **核心五张表**：`企业 → 项目 → 资金` + `节点`（到期提醒）+ `dict_item`（可配置枚举）。
- **数据出口**：对外暴露一个**只读 MCP server**，让 Codex/WorkBuddy 等 AI 工具读库，把台账数据自动填到其他科室要的各种表格里（AI 只能读、不能改）。
- **四条底线**（做不到就算失败）：不漏节点提醒、资金统计/勾稽不出错、查全貌不费劲、数据可备份不丢失。

---

## 2. 目标与非目标

### 2.1 目标

统一管理各级科技项目的 **企业、项目、财政资金** 三个维度，支撑：

1. **台账汇总**：跨层级（国家级/省级/苏州市级/昆山本级）、跨类型（科技成果转化、国际合作、创新联合体等）的项目统一登记与查询。
2. **全生命周期跟踪**：申报 → 立项 → 中期 → 验收 → 结题/绩效，每个项目当前卡在哪一步、下一个节点是什么，一目了然。
3. **节点提醒**：中期检查、验收、结题等节点到期前主动预警，不漏。
4. **资金管理**：每笔拨付的来源（上级拨付/本级配套/本级自付）、金额、批次、到账状态，以及三者勾稽关系核对。
5. **企业画像**：承担企业的资质、历史承担项目、累计金额。
6. **统计汇报**：按层级/类型/年度/企业/资金来源多维度汇总。
7. **数据出口（AI 填表）**：提供**只读** MCP 接口，让 Codex/WorkBuddy 等 AI 工具读取台账，按其他科室的表格要求自动填表（AI 只读、不参与台账写入与勾稽）。

### 2.2 非目标（明确不做）

- ❌ 不做申报/审批流程（各级官方系统已承担，本工具只做"承接后的管理"）。
- ❌ 不做多人协作、账号权限（单人使用）。
- ❌ 暂不自动对接/抓取各级官方系统（后期可留"Excel 导入"口子）。
- ❌ 不做移动端、不做云同步。
- ❌ MCP 出口**只读、不写**：AI 工具不能通过 MCP 修改台账数据（写入只能走台账界面）。

---

## 3. 验收标准（可验证）

| # | 标准 | 怎么验证 |
|---|---|---|
| A1 | 任意一个项目，几秒内查到：层级、类型、承担企业、总金额、资金来源构成、当前阶段、下一节点 | 项目详情页一眼可见 |
| A2 | 任意一个企业，能看到其承担的全部项目、历史 | 企业画像页 |
| A3 | 任意一笔资金能查到来源/金额/批次/到账状态，且「上级+配套+自付」勾稽对得上、汇总不出错 | 资金台账 + 勾稽核对面板 |
| A4 | 中期/验收等到期前主动提醒，不漏 | 提醒面板（分级预警） |
| A5 | 数据可备份，单文件拷贝即恢复 | 数据库单文件 + 一键备份 |
| A6 | 低配办公电脑上启动快、操作流畅 | 启动 1~2 秒、内存几十 MB、点击即时响应 |
| A7 | Codex/WorkBuddy 能通过 MCP **只读**读取台账；MCP 无任何写操作 | 连接 MCP 后只能查询，写入请求被拒 |
| A8 | 能通过配置界面自助新增/停用类别，无需改代码；停用不影响历史数据显示 | 配置管理视图操作生效 |

---

## 4. 领域模型

### 4.1 实体与关系

```
企业 (Enterprise) 1 ──承担── N 项目 (Project) 1 ──关联── N 资金 (Funding)
                                        │
                                        └──包含── N 节点 (Node)
```

### 4.2 生命周期状态机（项目的「阶段」）

```
申报中 → 已立项 → 实施中 → 待验收 → 已验收 → 绩效跟踪 → 已完结
                （实施中含中期检查）        （可随时进入异常态：中止 / 撤销）
```

### 4.3 资金勾稽规则（核心业务规则）

一个项目的资金，由三种来源构成：

| 来源 | 含义 |
|---|---|
| **上级拨付** | 国家/省/市上级财政下达的资金 |
| **本级配套** | 按上级下达文件要求，本级（昆山）配套的资金，通常与上级拨付存在比例关系 |
| **本级自付** | 本级自主立项、全额自付的资金 |

**勾稽关系**：

```
项目应到位资金 ≈ Σ(上级拨付) + Σ(本级配套) + Σ(本级自付)
本级配套  = 上级拨付 × 配套比例（比例由用户在每个项目上手工录入）
```

> 说明：配套比例不自动套政策公式，由你录入（不同项目政策不同）。系统只做**核对**：已录资金合计与项目总金额是否一致；不一致时标黄提醒（可能是批次未录完，属正常，需人工确认）。

### 4.4 关键不变量（数据不允许出现的状态）

- 一笔资金必须挂在某个项目下（不能悬空）。
- 一个项目必须有一个承担企业。
- 项目「阶段」只能按状态机流转，不能跳跃（异常态除外）。
- 资金来源类型只能取 `dict_item` 中 `funding_source` 定义的取值（默认三种）。

### 4.5 机制 vs 内容（可配置边界）

> 核心原则：**机制稳定，内容易变**。易变的枚举取值不写死在代码里，而是存进配置表，由你通过配置界面自助维护。

| 类别 | 举例 | 处理方式 |
|---|---|---|
| **机制**（稳定） | 实体关系、生命周期状态机 `stage`、资金勾稽规则 | 写在代码/表结构里，不开放配置 |
| **内容**（易变） | 层级、类型、资金来源、节点类型、区镇、企业类型的**取值清单** | 存 `dict_item` 配置表，可自助增/停用 |

- **新增**：配置界面加一条 → 全系统下拉框立即生效。
- **停用**：`is_active=0` → 不再出现在下拉框，但已用该值的历史数据仍正常显示（因存的是文本值）。
- **改名**：政务数据不追溯篡改历史——历史项目保留立项当时的类别名；如需改名，用「停用旧值 + 新增新值」实现。

---

## 5. 数据库设计（5 张表）

> 金额单位统一为 **万元**，日期统一为 `YYYY-MM-DD`。

### 5.1 企业表 `enterprise`

| 字段 | 类型 | 说明 |
|---|---|---|
| id | INTEGER | 主键 |
| name | TEXT | 企业名称（必填） |
| credit_code | TEXT | 统一社会信用代码（唯一） |
| enterprise_type | TEXT | 企业类型（取值由 `dict_item` 配置表维护） |
| qualifications | TEXT | 资质（如「高企2024、省专精特新」） |
| district | TEXT | 区镇（取值由 `dict_item` 配置表维护，默认 11 个） |
| contact_person | TEXT | 联系人 |
| contact_phone | TEXT | 联系电话 |
| address | TEXT | 地址 |
| note | TEXT | 备注 |
| created_at / updated_at | TEXT | 时间戳 |

### 5.2 项目表 `project`

| 字段 | 类型 | 说明 |
|---|---|---|
| id | INTEGER | 主键 |
| name | TEXT | 项目名称（必填） |
| project_no | TEXT | 项目编号 / 立项文号 |
| level | TEXT | 层级（取值由 `dict_item` 配置表维护） |
| category | TEXT | 项目类型（取值由 `dict_item` 配置表维护） |
| enterprise_id | INTEGER | 承担企业（外键） |
| total_amount | REAL | 项目总金额（万元） |
| start_date | TEXT | 开始日期 |
| end_date | TEXT | 结束日期 |
| stage | TEXT | 当前阶段（状态机取值） |
| match_ratio | REAL | 配套比例（如 1 表示 1:1，仅上级+配套类项目用） |
| leader | TEXT | 项目负责人 |
| contact_phone | TEXT | 联系人手机号 |
| note | TEXT | 备注 |
| created_at / updated_at | TEXT | 时间戳 |

### 5.3 资金表 `funding`

| 字段 | 类型 | 说明 |
|---|---|---|
| id | INTEGER | 主键 |
| project_id | INTEGER | 所属项目（外键） |
| source_type | TEXT | 资金来源（取值由 `dict_item` 配置表维护） |
| amount | REAL | 金额（万元） |
| batch | TEXT | 批次（首期 / 二期 / 尾款…） |
| plan_date | TEXT | 应拨时间 |
| actual_date | TEXT | 实拨时间 |
| status | TEXT | 未拨付 / 已拨付 / 已到账 |
| note | TEXT | 备注 |

### 5.4 节点表 `node`

| 字段 | 类型 | 说明 |
|---|---|---|
| id | INTEGER | 主键 |
| project_id | INTEGER | 所属项目（外键） |
| node_type | TEXT | 节点类型（取值由 `dict_item` 配置表维护） |
| plan_date | TEXT | 计划时间 |
| actual_date | TEXT | 实际完成时间 |
| status | TEXT | 待办 / 已完成 / 已逾期 |
| has_major_change | INTEGER | 是否发生重大事项变更（0=否 / 1=是） |
| note | TEXT | 备注 |

### 5.5 配置表 `dict_item`（可枚举取值的唯一来源）

| 字段 | 类型 | 说明 |
|---|---|---|
| id | INTEGER | 主键 |
| dict_type | TEXT | 字典类型：level / category / funding_source / node_type / district / enterprise_type |
| value | TEXT | 取值名称（如「概念验证中心」） |
| sort_order | INTEGER | 排序号 |
| is_active | INTEGER | 1=启用 / 0=停用（停用而非删除，保护历史引用） |
| created_at | TEXT | 时间戳 |

**种子数据（初始取值，均可后续自助增改）**：

| dict_type | 初始值 |
|---|---|
| level（层级） | 国家级 / 省级 / 苏州市级 / 昆山本级 |
| category（类型） | 科技成果转化 / 国际合作 / 创新联合体（政策调整后可自助新增，如概念验证中心、重点实验室…） |
| funding_source（资金来源） | 上级拨付 / 本级配套 / 本级自付 |
| node_type（节点类型） | 申报 / 立项 / 中期检查 / 验收 / 结题 / 绩效评价 |
| district（区镇） | 开发区 / 高新区 / 花桥 / 张浦 / 周市 / 陆家 / 巴城 / 千灯 / 周庄 / 淀山湖 / 锦溪（11 个） |
| enterprise_type（企业类型） | 高新技术企业 / 科技型中小企业 / 规上工业 / 其他 |

---

## 6. 界面设计（6 个视图）

| 视图 | 功能 |
|---|---|
| 📋 **项目总览** | 项目列表 + 筛选（层级/类型/阶段/企业）；详情页：项目全貌、阶段时间线、资金构成、下一节点 |
| 🏢 **企业画像** | 企业列表 + 详情：资质、承担的全部项目、累计金额 |
| 💰 **资金台账** | 每笔拨付明细（来源/金额/批次/到账）+ 勾稽核对面板 + 按来源汇总 |
| ⏰ **节点提醒** | 未来 30 天内到期 + 全部逾期节点；≤7 天红、≤30 天黄、逾期深红 |
| 📊 **统计报表** | 按层级/类型/年度/企业/资金来源汇总，可导出 |
| ⚙️ **配置管理** | 维护各字典取值（层级/类型/资金来源/节点类型/区镇/企业类型）：新增、停用、排序 |

---

## 7. 技术架构

### 7.1 技术选型

| 层 | 选择 | 理由 |
|---|---|---|
| 数据 | SQLite（单文件） | 零配置、单文件即备份、几十~上百项目绰绰有余 |
| 后端 | Python 标准库（`http.server` + `sqlite3`） | 零第三方依赖，装好 Python 即用，好交接 |
| 前端 | 单页 HTML + 原生 JS + CSS | 离线可用，无 CDN 依赖，简单可维护 |
| MCP 出口 | `mcp_server.py`（只读查询工具），用官方 `mcp` SDK | 标准 MCP 协议，Codex/WorkBuddy 等通用；只读不写；引入 `mcp` 一个依赖 |

> 明确**不用**大框架（Flask/Django/Vue/React）的原因：单人、离线、数据量小，框架带来的依赖安装和维护成本大于收益；标准库方案「双击即用、好交接、不依赖网络装包」。

**性能与资源约束（针对低配办公电脑）**：

- 约束：单位电脑性能一般，工具必须**轻量、低资源占用、操作瞬时响应**。
- 本方案的资源预期：启动 1~2 秒、内存占用几十 MB、界面点击即时响应、全程离线。
- 刻意规避会在低配电脑上吃资源的做法：Electron 桌面壳、Vue/React + 打包构建、独立数据库服务、云同步。
- 数据量几十~上百条对 SQLite 而言是"瞬秒"，无需任何性能优化；真正决定卡不卡的是技术栈重量，而非数据量。若未来数据涨到上万条，SQLite 仍可承受（再往上才需考虑换库/建索引），远在本工具需求之外。

### 7.2 目录结构

```
科技项目台账/
├── start.bat          # 双击启动（自动开浏览器）
├── app.py             # 后端：静态文件 + JSON API
├── mcp_server.py      # 只读 MCP server（供 AI 工具读库填表）
├── schema.sql         # 建表脚本
├── data/
│   └── project.db     # SQLite 数据（拷贝此文件即备份）
├── static/
│   ├── index.html
│   ├── app.js
│   └── style.css
├── .vibe/             # 项目治理目录（进度/契约/任务）
└── 设计方案.md        # 本文档
```

### 7.3 数据流

```
浏览器(本地) ──HTTP──▶ app.py(标准库) ──SQL──▶ SQLite(project.db)
      ▲                                              │
      └────────────── JSON 结果 ◀────────────────────┘

Codex/WorkBuddy ──MCP(stdio)──▶ mcp_server.py ──只读SQL──▶ SQLite(project.db)
```

启动方式：双击 `start.bat` → 自动启动本地服务 → 自动打开 `http://localhost:端口`。

---

## 8. 实施计划（5 阶段）

| 阶段 | 内容 | 交付物 |
|---|---|---|
| 1 骨架 | 建目录、数据库 schema（5 张表 + 种子数据）、启动脚本、只读查询层 | 能启动的空系统 + 5 张表 + 查询函数 |
| 2 最小闭环 | 企业/项目/资金的增删改查 + 项目全貌查询 | 一条「录入→查全貌」的端到端链路 |
| 3 补全 | 节点提醒、资金勾稽核对、统计报表、企业画像、配置管理界面 | 全部 6 个视图可用 |
| 4 MCP 只读出口 | mcp_server.py，只读查询工具，对接 Codex/WorkBuddy | AI 工具能读库填表 |
| 5 硬化 | 数据备份、Excel 导入、错误处理 | 可日常使用 |

> 原则：先跑通第 2 阶段「最小闭环」给你看，确认字段贴合你的实际，再往 3、4、5 阶段扩。避免一上来做全、结果字段对不上返工。MCP（阶段 4）依赖数据库结构稳定，故排在补全之后。

---

## 9. 风险与合规

| 风险 | 应对 |
|---|---|
| 数据丢失 | SQLite 单文件，定期拷贝即备份；阶段 4 加一键备份脚本 |
| 数据外泄 | 纯本地运行，不联网、不上云，无外泄面 |
| 字段与实际台账不符 | 阶段 2 最小闭环先给你看，尽早对齐；支持后续加字段 |
| 纸质数据无法自动录入 | 需手工录入（Excel 可阶段 4 导入） |
| 换电脑 / 交接 | 拷贝整个文件夹即可迁移 |
| AI 填表可能填错数字 | AI 只读、只能产出"初稿"；金额/日期等关键字段人工核对后再交 |

---

## 10. 假设与待定决策

**假设**（后续验证）：
- 数据量几十~上百个项目/企业，SQLite 足够。
- 配套比例由用户手工录入，系统不自动套政策公式。
- 金额单位统一为「万元」。

**待定（请你拍板）**：
- [ ] 各字典的**初始种子值**是否如上（类型现为 3 档，政策调整后可自助新增）？初始给多少、够不够？
- [ ] 资金是否还有**第 4 种来源**（如企业自筹、社会资本）？当前只设了上级/配套/自付三种。
- [ ] 节点类型是否就这 6 种（申报/立项/中期检查/验收/结题/绩效评价）？
- [ ] 项目表和企业表还有没有你实际台账里**必须有的字段**（如主管科室、经费预算科目、项目组成员等）？
- [ ] 是否需要一个「**附件/文档**」关联能力（给每个项目挂扫描件、批文 PDF 等）？本方案暂未包含。

---

## 11. 请你重点审查的 6 处

1. **数据库 4 张表的字段**（第 5 节）—— 这是全系统地基，字段对不对、全不全最关键。
2. **资金勾稽规则**（4.3）—— 你的实际拨付是不是"上级+配套+自付"三种、配套是否按比例，理解对不对。
3. **生命周期状态机**（4.2）—— 阶段划分和流转是否符合你的实际流程。
4. **非目标**（2.2）—— 确认「不做申报审批、不做多人、不自动对接」这三条边界你能接受。
5. **待定决策清单**（第 10 节）—— 尤其是各字典的初始种子值。
6. **MCP 只读出口**（第 7、8 节）—— 确认「AI 只读、不写」这个边界，以及装官方 `mcp` SDK 是否可接受。
